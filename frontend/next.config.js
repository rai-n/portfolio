/** @type {import('next').NextConfig} */
const nextConfig = {

    output: 'export',

    images: {
        domains: ['cdn1.iconfinder.com', 'cdn4.iconfinder.com'], // Add other domains if needed
    },
}

module.exports = nextConfig

